{
  "name": "tests",
  "version": "1.0.0",
  "description": "Simple tests for Medical Appointments MVP",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:performance": "k6 run load-test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}